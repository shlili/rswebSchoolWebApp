@model MVCSchoolApp.ViewModels.CreateNewAccountVM


@{
    ViewData["Title"] = "Create new account";
}

<h1>Edit</h1>

<h4>Teacher</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateNewAccount">
            <div class="form-group">
<label class="control-label">Actors</label>
<select asp-for="SelectedTeacher" class="form-control js-example-basic-multiple"
asp-items="Model.TeacherList"> </select>
<span asp-validation-for="SelectedTeacher" class="text-danger"></span>
</div>
           
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}